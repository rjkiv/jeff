- name: mainCRTStartup
  pdata_type: 2 # C - in pdata, C exceptions
  num_handlers: 1
  # no size, no signature, we don't need those to know we're at the entrypoint
  references:
    - name: __savegprlr_28
      kind: Label
      size: 0x4
      optional: false
    - name: __onexitend
      kind: Object
      size: 0x4
      optional: false
    - name: __onexitbegin
      kind: Object
      size: 0x4
      optional: false
    - name: XapiInitProcess
      kind: Function
      # no size parameter because we don't know it
      optional: false
    - name: XapiCallThreadNotifyRoutines
      kind: Function
      # no size parameter because we don't know it
      optional: false
    - name: XapiPAL50Incompatible
      kind: Function
      # no size parameter because we don't know it
      optional: false
    - name: XamTerminateTitle
      kind: Function
      # no size parameter because we don't know it
      # TODO: add flag for being an import?
      optional: false
    - name: _mtinit
      kind: Function
      # no size parameter because we don't know it
      optional: false
    - name: _rtinit
      kind: Function
      # no size parameter because we don't know it
      optional: false
    - name: _cinit
      kind: Function
      # no size parameter because we don't know it
      optional: false
    - name: _CRTCommandLineArgs
      kind: Object
      size: 0x4
      optional: false
    - name: GetCommandLineA
      kind: Function
      # no size parameter because we don't know it
      optional: false
    - name: main
      kind: Function
      # no size parameter because we don't know it
      optional: false
    - name: _cexit
      kind: Function
      size: 0xC
      optional: false
    - name: ??_C@_0BI@IDEPEPLM@?$FLXAPI?5RETURN?5VALUE?$FN?5?$CFd?6?$AA@
      kind: Object
      size: 0x18
      optional: false
    - name: DbgPrint
      kind: Function
      # no size parameter because we don't know it
      optional: false
    - name: XamTerminateTitle
      kind: Function
      # no size parameter because we don't know it
      # TODO: add flag for being an import?
      optional: false
    - name: UnhandledExceptionFilter
      kind: Function
      # no size parameter because we don't know it
      optional: false